// TODO: What will the following line of code log?
console.log(window);

// console. log() restricts your code to work in environments where window is the global object (or a property of the global object).

// logs the window object in the browser

// TODO: What will the following line of code log?
console.log(window.document);

// window.document returns a reference to the document contained in the window.


// TODO: What will the following line of code log?
console.log(document.documentElement);

// Document.documentElement returns the Element that is the root element of the document (for example, the <html> element for HTML documents). returning from line 2.HTML

// TODO: What will the following line of code log?
console.log(document.head);

//The head read-only property of the Document interface returns the <head> element of the current document. 


